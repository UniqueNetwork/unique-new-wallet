(function (w) {

    w.ENV = w.ENV || {};

    w.ENV.IPFS_GATEWAY = '${IPFS_GATEWAY}';
    w.ENV.SCAN_ACCOUNT_URL = '${SCAN_ACCOUNT_URL}';
    w.ENV.UNIQUE_API_URL = '${UNIQUE_API_URL}';
    w.ENV.NET_QUARTZ_NAME = '${NET_QUARTZ_NAME}';
    w.ENV.NET_QUARTZ_API = '${NET_QUARTZ_API}';
    w.ENV.NET_OPAL_NAME = '${NET_OPAL_NAME}';
    w.ENV.NET_OPAL_API = '${NET_OPAL_API}';

})(this);
