import { css } from 'styled-components';

export const PaddedBlock = css`
  padding: calc(var(--gap) * 2);
`;

export const LisFooter = css`
  align-items: center;
  display: flex;
  justify-content: space-between;
  padding-top: calc(var(--gap) * 2);
`;
