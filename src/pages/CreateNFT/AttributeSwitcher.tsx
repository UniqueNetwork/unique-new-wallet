import { FC } from 'react';

import { AttributeType } from './types';

interface AttributeSwitcherProps {
  type: AttributeType;
}

// const AttributeSwitcher: FC<AttributeSwitcherProps> = ({ type, children }) => {
//     React.Children.
// };
