import styled from 'styled-components';
import {PagePaper} from "@app/components";

const MyTokensComponent = () => {
  return (
    <PagePaper>
      <div>My Tokens</div>
    </PagePaper>
  );
};

export const MyTokens = styled(MyTokensComponent)``;