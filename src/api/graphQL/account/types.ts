export type AccountCommonInfoResponse = {
  view_collections_aggregate?: {
    aggregate: {
      count: number;
    };
  };
  view_tokens_aggregate?: {
    aggregate: {
      count: number;
    };
  };
};
